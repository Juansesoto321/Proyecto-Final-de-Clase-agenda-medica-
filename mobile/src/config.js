// IMPORTANTE: cuando pruebes con Expo Go en un celular físico, "localhost"
// apunta al propio celular, no a tu computador. Debes poner aquí la IP local
// de tu PC (la misma red WiFi que el celular). Para hallarla en Windows:
//   ipconfig   ->  busca "Adaptador de LAN inalámbrica Wi-Fi" -> "Dirección IPv4"
//
// Si usas el emulador de Android (no un celular físico), usa 10.0.2.2 en vez
// de la IP de tu PC.
export const API_BASE_URL = 'http://172.20.10.3:3000/api';
